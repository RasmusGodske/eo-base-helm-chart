{{- range $key, $value := .Values.deployments}}
---
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "ett-base-helm-chart.fullname" $ }}-container-template-test
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  {{- include "eo-base-helm-chart.podTemplate" (dict "root" $ "name" $key "deployment" $value) | nindent 2 }}
{{- end }}
