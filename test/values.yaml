env: {}

envSecrets:
  TEST_SECRET:
    secretName: test
    key: test-key

deployments:
  test:
    replicaCount: 1
    args: "exit $([ ! -z $TEST_SECRET ] && [ $TEST_SECRET == 'test-value2' ])"
    env: {}
    image:
      repository: "ubuntu"
      tag: "latest"
      pullPolicy: Always

