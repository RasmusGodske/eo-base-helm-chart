env: {}

envSecrets:
  TEST_SECRET:
    secretName: test
    key: test

deployments:
  - name: test
    replicaCount: 1
    args: "[ ! -z $TEST_SECRET ] && [ $TEST_SECRET == test ]"
    env: {}
    image:
      repository: "ubuntu"
      tag: "latest"
      pullPolicy: Always

