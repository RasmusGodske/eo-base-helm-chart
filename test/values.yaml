env: {}

envSecrets:
  TEST_SECRET:
    secretName: test
    key: test-key

deployments:
  test:
    replicaCount: 1
    args: "[ ! -z $TEST_SECRET ] && [ $TEST_SECRET == test-value ]"
    env: {}
    image:
      repository: "ubuntu"
      tag: "latest"
      pullPolicy: Always

